{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|grep",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'file=$(cat | jq -r \".tool_input.file_path // empty\"); if echo \"$file\" | grep -qE \"(\\.env$|\\.env\\.)\"; then echo \"Blocked: reading .env files is not allowed\"; exit 2; fi'"
          }
        ]
      }
    ]
  }
}